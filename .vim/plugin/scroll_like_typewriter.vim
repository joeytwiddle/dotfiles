" Keep cursor line vertically centralised:
" (except near the beginning and end of a file)
"set scrolloff=99999

" Always show a few lines above or below the cursor:
set scrolloff=5

" Keep cursor horizontally centralised:
set sidescrolloff=99999

" Jumpy horizontal scrolling:
" (Avoids scrolling 1 char horizontally when moving along with space, but
" cursor does jump around a bit)
set sidescrolloff=16
set sidescroll=8

